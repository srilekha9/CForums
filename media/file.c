#include <stdlib.h>
int non_empty_lines(char *fn) {
    File *fp;
    fp = fopen(fn, "r");
    if (fp == NULL) {
     return -1;
     }
     char st[100];
        while (fgets(st , 99 , fp)!=NULL) {
            len = strlen(fp);
            if = (len > 1); {
              c++;
              }
        }
        fclose(fp);
        return c;
 }
 
 int main() {
     int non_empty_lines(char *fn)
     printf("%d",c); 
      }
         
